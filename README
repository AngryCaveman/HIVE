这是本人独立开发的一个数据采集系统，我给它起名叫做Hive（蜂巢），分为客户端和服务端两个部分。
1，客户端基于C#的winform程序。
2，服务端是基于Scrapy框架的爬虫程序。
客户端并非直接访问服务端，而是先将配置信息存入中间数据库，由服务端定时从中调度，属于注册机制。
之后的版本，会写一个服务端取代中间数据库，这样才是我最初设想的那样，但是我不知道怎样用python写一个高效的服务器模块（C#还行）。目前正在看Twisted。
